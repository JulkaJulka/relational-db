CREATE TABLE FORUM_SUBCATEGORIES(
ID NUMBER,
CONSTRAINT FOR_SUBCATEGORY_PK PRIMARY KEY(ID),
ID_CATEGORY NUMBER,
CONSTRAINT FORUM_CATEGORY_FK FOREIGN KEY(ID_CATEGORY) REFERENCES FORUM_CATEGORIES(ID),
TITLE VARCHAR(45),
DESCRIPTION CLOB,
"DATE" TIMESTAMP,
IP VARCHAR(20)
);